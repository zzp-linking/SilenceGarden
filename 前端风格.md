# 静谧花园 · 前端风格规范

> 本文档记录 Silence Garden 当前前端的视觉方向、组件语言和交互约定。后续新增页面、重构页面或制作新组件时，应优先复用这里的规则，保持整个网站像同一座持续展开的数字园林。

## 1. 核心定位

网站不是传统的个人简历或产品后台，而是一个“半匿名的数字园林”。访客通过文字、作品、代码和审美感知创建者，不需要强行展示完整个人身份。

核心气质：

- 古典、文艺、含蓄，但不是陈旧复古。
- 有生命力、自然、舒展、清新，并且保留呼吸感。
- 像题记、诗句、印章和长卷，需要访客自行体会，而不是一次性解释所有内容。
- 页面允许向下展开，访客像走进一座园林，依次经过不同区域。
- 桌面端优先，允许更大的画面、横向构图和较复杂的互动；移动端保持阅读和导航的完整性。

网站名称固定为：

- 中文：静谧花园
- 英文：Silence Garden
- 可使用的题字：静、阅、诗等单字印章

## 2. 视觉关键词

推荐始终围绕以下词汇做设计判断：

`纸张` · `墨色` · `竹叶` · `苔痕` · `溪水` · `留白` · `长卷` · `题字` · `印章` · `琴室` · `工坊`

不应让页面滑向以下方向：

- 赛博朋克、霓虹渐变、过度发光。
- 商业 SaaS、数据看板、模板化卡片墙。
- 大面积纯白背景、厚重圆角卡片、强烈玻璃拟态。
- 过度装饰、过多动效、为了“炫技”而打断阅读。

## 3. 色彩系统

### 3.1 主色板

| 用途 | 色值 | 说明 |
| --- | --- | --- |
| 宣纸底色 | `#f2eee3` | 页面默认背景，避免使用纯白 |
| 深纸色 | `#e6e1d3` | 代码、浅色辅助区域或纸张层次 |
| 主墨色 | `#1e2924` | 主标题、正文和重要文字 |
| 柔墨色 | `#405149` | 次要正文、说明文字 |
| 淡墨色 | `#718078` | 辅助信息、摘要、空状态 |
| 青绿色 | `#5f806c` | 植物、溪水、链接和一般交互 |
| 深青绿 | `#315746` | 主要按钮、强调链接、深色区域 |
| 浅青绿 | `#a9c0a8` | 背景氛围、弱边界、装饰层 |
| 朱砂红 | `#a94737` | 印章、题字强调、悬浮/焦点反馈 |
| 古金色 | `#c6a15b` | 线条、唱片边框、章节标记和少量高光 |
| 深色面板 | `#19221e` | 琴室、工坊或需要反差的作品区域 |

### 3.2 使用原则

- 页面默认使用暖纸色，不使用 `#fff` 作为整体背景。
- 青绿色是主导氛围色，朱砂红和古金色只做点题，不应大面积铺开。
- 页面层次主要通过透明度、细线、阴影和渐变建立，而不是通过厚重边框建立。
- 深色区域可以用于“工坊”“琴室”等特殊空间，但应保留青绿、古金和纸色文字，使其仍属于同一座花园。
- 新增颜色前先确认是否能归入以上色板；尽量修改共享变量而不是在单个页面随意创造新颜色。

共享样式入口：`SilenceGarden/src/config/garden.less`。

## 4. 背景和材质

### 4.1 花园背景

普通内容页使用 `.garden-surface` 作为页面基底：

- 暖宣纸底色。
- 左上和右下使用低透明度的径向渐变，模拟光线与植物气息。
- 使用极低对比度的颗粒点阵模拟纸张纹理。
- 纹理层必须 `pointer-events: none`，不能影响点击和滚动。
- 内容层保持在纹理之上，避免背景装饰遮挡文字。

### 4.2 内容面板

- 文章和诗词详情可以使用半透明纸页层，例如 `rgba(255, 255, 255, .38)`，但不能变成突兀的白色大卡片。
- 内容面板可配合细青绿边框和偏移阴影，模拟纸页压在桌面上的感觉。
- 边框优先使用 1px 低透明度线；避免粗边框和强烈投影。
- 代码块应融入所在模块的背景。不要在代码内部叠加一块明显不同色的背景，也不要无必要地添加边框。
- Markdown 内容的现有自定义样式应优先保留；调整时只改确有问题的局部，避免重新套用明显的通用 Markdown 白色主题。

## 5. 字体和文字排版

### 5.1 字体角色

- 中文标题、诗词、题记、章节名：优先使用项目手写/信笺字体 `信笺手写体`，回退到 `楷体`、`微软雅黑`。
- 英文小标签、编号、技术栈、时间：使用等宽字体或系统无衬线字体，例如 `ui-monospace`、`Consolas`。
- 箭头、音乐符号等少量符号可使用 Georgia 或系统衬线字体。

项目已有字体变量：`@poem`，定义于 `SilenceGarden/src/config/base.less`。

### 5.2 排版规则

- 标题使用较轻的字重，避免粗黑体；通过字号、留白和颜色建立层级。
- 中文标题适当增加字间距，英文标签使用大写和较宽字距。
- 正文保持舒展的行高，诗词和文章阅读以舒适为优先。
- 说明文字、元数据、时间和编号应比标题弱，使用淡墨色或低透明度。
- 题字和诗句可以保留含蓄，不要用过多解释性副标题破坏留白。
- 标题不要全部依赖大字号；大字号必须和足够的留白、细线或印章搭配。

## 6. 布局和页面结构

### 6.1 长卷式首页

首页是整个网站的精神入口，采用“向下展开的长卷”结构：

1. 入口/题记：展示“静谧花园”和一句含蓄的引子。
2. 进入路径：诗词、文章、琴室等入口以开放式列表出现。
3. 工坊：集中展示项目、技术栈与链接。
4. 代表性实验：首页只展示一个最有代表性的互动实验，例如坦克世界开箱推演。
5. 项目簿或余韵区域：展示更多项目线索，并把访客带回园中。

可以继续发散新的区域，但每个区域都应有明确的气质变化和内容理由，不能只是把模块纵向堆叠。

### 6.2 普通列表页

诗词、文章、音乐列表页沿用统一的“目录/册页”语言：

- 页面顶部使用统一站点头部：左侧品牌印章，右侧章节标签和返回链接。
- 首屏使用标题、题记式说明、数量或状态信息。
- 列表采用带细线的横向条目，而非密集卡片网格。
- 条目包含编号、主标题、摘要/作者/专辑等元信息和右侧箭头。
- 悬浮时可轻微增加左右内边距、改变背景透明度、突出底线和箭头；动作要克制。
- 空状态使用含蓄的短句，不使用强烈的错误或营销文案。

### 6.3 详情页

文章详情和诗词详情应像一页被单独展开的稿纸：

- 桌面端使用左侧侧注/印章 + 右侧主要内容的双栏结构。
- 侧注可使用 `position: sticky`，帮助保持空间感，但不能遮挡正文。
- 主内容有明确的标题区、内容区和页尾。
- 详情页进入时滚动位置必须在顶部；从列表返回时，列表页保留原滚动位置。
- 长内容提供符合项目风格的“一键回到顶部”按钮。

## 7. 组件语言

### 7.1 品牌头部

- 品牌印章使用朱砂红色、小幅旋转，文字使用手写字体。
- 中文品牌名旁显示英文 `Silence Garden`，英文作为辅助信息而不是主标题。
- 导航按钮使用细边框、较小圆角或胶囊形，避免厚重实心导航栏。
- 返回链接的文字要清楚，但视觉权重低于页面标题。

### 7.2 印章、题字和装饰线

- 印章是页面识别元素，可使用“静”“诗”“阅”等单字。
- 印章允许轻微旋转，不要每个印章使用相同角度。
- 章节标签前可以使用短朱砂线；编号和标签使用等宽字体。
- 装饰线优先使用细线、断线、圆环和留白，不使用复杂图标堆砌。

### 7.3 按钮和链接

- 主要操作使用深青绿或古金色，文字保持清楚。
- 次要操作使用透明背景和细边框。
- 悬浮反馈以颜色、轻微位移、底线或背景透明度变化为主。
- 所有可交互元素必须提供 `:focus-visible` 状态。
- 链接和按钮的箭头可以向右上方轻微位移，表示“进入另一间房”。

### 7.4 回到顶部

文章和诗词详情页统一使用回到顶部按钮：

- 页面滚动约 420px 后显示。
- 使用 `window.scrollTo({ top: 0, left: 0, behavior: 'smooth' })` 实现平滑回顶。
- 检测 `prefers-reduced-motion: reduce`，用户要求减少动态时改为即时滚动。
- 视觉上使用小型竖向纸签/印章语言：暖纸色半透明背景、青绿文字、古金短线、轻微偏移阴影。
- 按钮固定在右下角，不能遮挡正文、播放器或重要操作。
- 组件卸载时移除滚动监听。

## 8. 动效和滚动

### 8.1 动效基调

动效应像水墨、风、纸张和景物的位移：慢、轻、连续，有空间层次。不要使用弹跳、霓虹、快速缩放等与整体气质冲突的效果。

推荐动效：

- 章节进入时淡入并轻微上移。
- 背景圆环、竹叶、池水和雾气产生低幅度视差。
- 列表条目悬浮时轻微移动和变色。
- 图片预览使用轻微缩放淡入。
- 首页章节之间使用中等强度的水墨层叠、景物位移和章节转场。

### 8.2 首页滚动

- 桌面端允许整屏章节和横向构图。
- 使用 `scroll-snap-type: y proximity` 或同等“接近吸附”的策略，让章节有整屏感，但不能强制阻塞滚动。
- 章节使用 `scroll-snap-align: start`；不要依赖固定 `100vh` 作为唯一定位依据，避免 2K/4K、浏览器缩放和地址栏高度造成缝隙或无法滚动。
- 对滚轮滚动应保留自然累积距离，不能每次滚轮事件都强行跳整屏。
- 视差使用 CSS 变量或 `transform: translate3d(...)`，幅度要小，避免文字漂移和性能问题。
- 滚动相关监听应使用 passive listener，并在组件卸载时清理。

### 8.3 唱片动画

音乐列表的唱片规则：

- 唱片中心使用 `/home/assets/image/violin/catalog-disk-bg1.png`。
- 中心图片保持居中，使用绝对定位和 `translate(-50%, -50%)` 处理裁剪。
- 只有鼠标悬浮在曲目条目上时旋转。
- 使用匀速、无限循环的 `linear infinite`，当前速度约为 6 秒一圈。
- 鼠标移开后取消动画并回到静止状态。
- 用户启用减少动态效果时必须禁用旋转。

### 8.4 动效可访问性

统一支持：

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

对于滚动、旋转、视差等动效，除了 CSS 媒体查询，也应在 JavaScript API 调用前检查该设置。

## 9. 响应式规则

断点沿用项目已有的 `@threshold: 768px`。

桌面端：

- 以较宽内容区、双栏结构、横向构图和更大的留白为主。
- 首页章节可以使用 `100svh`，但要为极端屏幕高度保留最小高度和内容安全空间。
- 允许复杂背景装饰和较明显的视差，但文字和主要操作必须稳定。

移动端：

- 导航标签可隐藏，保留品牌和返回操作。
- 双栏详情页改为单栏，侧注改为正文前的普通区块。
- 列表压缩编号、图片和间距，但保留编号、标题、箭头这条信息主线。
- 首页取消强制整屏吸附，使用自然滚动或 proximity 吸附。
- 装饰元素可以裁切、缩小或降低透明度；不能覆盖内容。
- 回到顶部按钮缩小并避开屏幕边缘，保留足够的触控面积。

## 10. 内容和信息架构

- 首页只突出一个最有代表性的实验，避免多个项目同时争夺注意力。
- 编程实验、互动玩具、个人项目三者都可以存在，但集中收纳在“工坊”或项目簿区域。
- 开源项目应明确展示项目名称、技术栈、状态和链接。
- 文章、诗词和音乐属于可独立进入的空间，列表页负责引导，详情页负责沉浸式阅读/聆听。
- 音乐默认静音；环境声只能作为可选氛围层，由访客主动开启。
- 题字、印章、章节编号可以明确展示，个人身份信息保持克制。

## 11. 实现约定

- 新页面优先使用 `garden-surface`、`garden-site-header`、`garden-brand`、`garden-page-main`、`garden-hero`、`garden-list-heading`、`garden-empty` 等共享类。
- 共用颜色和字体优先引用 `garden.less`、`base.less` 中的变量。
- 路由切换后的详情页默认回到顶部；需要保留列表阅读进度时，由列表页自行保存和恢复。
- 图片资源统一遵守项目的 `/home/assets/...` 访问路径和现有代理规则，不在组件中硬编码生产域名。
- 内容页的 Markdown 样式属于用户可调整区域，重构时不要擅自覆盖用户已经确认的渲染规则。
- 优先使用 CSS 和轻量 JavaScript；只有确实能提升空间叙事时才引入更复杂的动画库。
- 每次涉及页面风格的修改后，至少执行一次前端构建，并检查桌面端、移动端、减少动态效果三种状态。

## 12. 新页面自检清单

- [ ] 是否仍然属于“静谧花园”，而不是通用模板或 SaaS 页面？
- [ ] 是否使用暖纸色、墨色、青绿色，并克制使用朱砂和古金？
- [ ] 是否有足够留白，信息层级是否通过字体、间距和细线表达？
- [ ] 是否复用了共享头部、标题、列表和空状态语言？
- [ ] 交互反馈是否轻柔、清楚，并且提供键盘焦点状态？
- [ ] 长内容是否支持平滑回顶或合理的阅读导航？
- [ ] 是否处理了滚动监听清理、`prefers-reduced-motion` 和移动端布局？
- [ ] 是否避免纯白大背景、厚重卡片、霓虹渐变和无意义动效？
